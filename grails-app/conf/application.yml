---
grails:
    profile: plugin
    codegen:
        defaultPackage: grails.cascade.validation
info:
    app:
        name: '@info.app.name@'
        version: '@info.app.version@'
        grailsVersion: '@info.app.grailsVersion@'
